const single = (input) => {

    if(input.status === SINGLE){
        if(input.client.oas){
            // Client paid on Table 1
        }else{
            // Client not entitled to GIS
        }
    }else{
        // return an error
    }

}